import 'package:flutter/material.dart';

class KNUSTFeedScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('KNUST Feed'),
      ),
      body: Center(
        child: Text('This is the KNUST Feed'),
      ),
    );
  }
}
