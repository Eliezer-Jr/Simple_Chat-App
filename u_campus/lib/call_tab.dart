import 'package:flutter/material.dart';

class CallTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Call Tab'),
      ),
      body: Center(
        child: Text('This is the Call Tab'),
      ),
    );
  }
}
