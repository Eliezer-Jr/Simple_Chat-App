import 'package:flutter/material.dart';

class GIMPAFeedScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('GIMPA Feed'),
      ),
      body: Center(
        child: Text('This is the GIMPA Feed'),
      ),
    );
  }
}
