import 'package:flutter/material.dart';

class UGFeedScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('UG Feed'),
      ),
      body: Center(
        child: Text('This is the UG Feed'),
      ),
    );
  }
}
